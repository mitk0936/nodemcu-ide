{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build:serialport": {
      "outputs": ["node_modules/@serialport/bindings/build/Release/**"]
    },
    "build": {
      "dependsOn": ["build:serialport"],
      "outputs": ["./dist/**"]
    }
  }
}
